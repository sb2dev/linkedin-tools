node_modules/
dist/
build/
coverage/
*.tsbuildinfo
*.log

# Where run.sh parks a build it cannot overwrite, after someone has run it with sudo.
.dist-unwritable-*/
.dist-root-owned/

.env
.env.local
.DS_Store
.idea/
.vscode/

# A private PostgreSQL started with initdb, for running without a system service.
.pgdata/

# The LinkedIn export is real breached personal data and must never be committed. The sample
# attachments are screenshots of the same profiles, so they are no more publishable than it is.
sample/
*.csv
*linkedin*.txt

.run/
